<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Detection needs no network — the model ships inside the artifact and
         nothing is ever fetched. This is for the usage turnstile, which posts one
         load event per device per day. See docs/USAGE.md; set DAL_USAGE_DISABLED
         to switch it off, and this permission can go with it. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="Tongue Example"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
